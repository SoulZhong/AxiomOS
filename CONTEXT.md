# AxiomOS

AI 原生的组织操作系统：组织成员设定目标、拆解为任务、分配给人或 Agent 执行，系统记录执行结果与资源消耗，并以甘特图、看板、迭代等视图呈现组织运行状况。

**产品形态**：可高度定制的 SaaS，同时支持私有化部署。管理策略由每个组织自己配置，系统只给默认值；只有保证算得对、不越权的规则才写死（ADR 0014）。

**用语原则**：所有面向用户的名称必须是一线员工不需要解释就能懂的日常中文。括号里的英文只是代码标识符，绝不出现在界面、通知、文档标题里。

## Language

### 组织与人

**账号**（code: `Account`）:
一个登录身份，可以是多个组织的成员。
_Avoid_: 用户、user、login

**组织**（code: `Organization`）:
使用本系统的一家公司或团体，是数据隔离的边界。组织可注销，注销后数据保留、不可访问。
_Avoid_: 租户、tenant（只在基础设施层使用）、公司、企业

**组织负责人**（code: `OrganizationOwner`）:
对整个组织负责的成员，拥有组织内全部权限，公共 Agent 归他所有。
_Avoid_: 管理员、超级管理员、CEO

**平台管理员**（code: `PlatformAdmin`）:
运营这套系统的人，用独立后台（/admin）创建和停用组织、维护全局价格表、查看各组织用量。不属于任何组织，也看不到组织内的任务。
_Avoid_: 超级管理员、系统管理员、root

**权限**（code: `Permission`）:
角色可以持有的五种组织级能力：组织设置、管理流程、取消任何任务、查看全部工作、查看全部成本。后两项是跨共享边界的例外。成员的权限是他所有角色权限的并集。
_Avoid_: 授权（授权专指给 Agent 的）、scope

**范围**（code: `Scope`）:
你正在看的是全公司还是某个团队（含其全部下级团队）。能切到哪些范围、每一档能否看到成本，由所在组织的两项**可见性策略**决定（ADR 0013）：协作数据默认全员可见，财务数据默认只对自己团队及下级开放。
_Avoid_: 数据权限、可见性、visibility

**可见性策略**（code: `VisibilityPolicy`）:
组织设置里的两个开关，分别管协作数据与财务数据，取值都是三选一：全员可见、按共享边界、只看自己团队及下级。系统只给默认值，每个组织自己定。
_Avoid_: 数据权限模型、ACL

**共享边界**（code: `SharingBoundary`）:
团队树上的一个标记。被标记的团队连同它下面的所有团队构成一个共享域，域内的人互相看得见，域与域之间看不见。一个人的可见域是他所在团队向上最近的那个边界所辖的整棵子树；没有边界时是全组织。用它可以表达"事业部之间不共享、事业部内共享"这类结构。
_Avoid_: 数据域、隔离域、租户（租户专指组织）

**成本归口**（code: `CostAttribution`）:
一条执行记录的成本算在执行者所属团队头上；执行者是 Agent 时算在其所有者所属团队；没有团队的算「未分组」。这是全系统唯一的口径。
_Avoid_: 分摊、成本中心

**邀请**（code: `Invitation`）:
组织负责人或有组织设置权限的成员发给某个邮箱的加入链接，14 天内有效，接受后成为成员。
_Avoid_: 注册、invite code、激活

**语言**（code: `Locale`）:
每个账号的界面语言（简体中文或英文），组织有默认语言；Agent 用所有者的语言。
_Avoid_: 地区、region、i18n

**团队**（code: `Team`）:
组织内可嵌套的人员分组，用于表达部门、小组层级，有一个团队负责人。
_Avoid_: 部门、group、department

**团队负责人**（code: `TeamLead`）:
团队的负责人，只决定他默认看到哪个范围的运行情况，不带来额外的修改权限。
_Avoid_: 经理、主管、manager

**成员**（code: `Member`）:
属于某个组织的一个人。
_Avoid_: 员工、用户、user、account

**角色**（code: `Role`）:
组织自定义的、带名称和权限的标签（如开发、测试、管理员），也是流程里判断"谁可以做这一步"和待领取任务匹配的依据。系统内置七个，组织可改可加。
_Avoid_: 职位、权限组

### Agent

**Agent**（code: `Agent`）:
成员注册到系统里、在系统外运行的自动化程序，替它的所有者干活；能做的事不超过所有者本人。
_Avoid_: 机器人、bot、worker、智能体

除组织负责人与持「组织设置」权限的成员外，每个人在 Agent 页只能看到并管理自己的 Agent，另加只读地看到公共 Agent。

**设备码**（code: `DeviceCode`，验证码 `UserCode`）:
Agent 接入向导用的一次性凭证（ADR 0018）：Agent 端执行一条接入命令，拿到只有它知道的设备码和一个 8 位验证码（`ABCD-1234`）；所有者在网页里输入验证码、选授权并批准，系统以批准人为所有者创建这个 Agent，Agent 端自动拿到令牌并写好客户端配置。15 分钟内有效，令牌只给一次。设备码绑定的是 Agent 身份，不是人的登录。
_Avoid_: 授权码、配对码、激活码、token（令牌是另一个东西）

**接入检查（Agent）**（code: `AgentCheck`）:
接入向导最后一步：回答"它连上了吗"——现在的状态（见「Agent 状态」）、最近一次活动、最近调用的工具，一句人话说明现在该做什么。
_Avoid_: 健康检查、探活、ping

**所有者**（code: `Owner`）:
对某个 Agent 负责的成员。Agent 做的事、花的钱都算在所有者头上。
_Avoid_: 创建者、注册人、主人

**公共 Agent**（code: `SharedAgent`）:
所有者是组织负责人、可以直接被指派任务的 Agent；普通 Agent 只替自己的所有者干活。
_Avoid_: 组织级 Agent、系统 Agent、全局 Agent

**Agent 状态**（code: `AgentState`）:
一个 Agent 现在是什么情况，三选一，只用于展示和提示，不影响指派：
- **执行中**（`running`）：名下有打开的执行记录，或最近三分钟内有过请求——它这会儿正在干活。
- **可用**（`ready`）：令牌有效、没有注销、所有者在职，只是这会儿没活干。这是**正常的休息状态**：
  通过 MCP 接入的 Agent 不是一条常连着的线，没事的时候本来就不说话，所以绝大多数时候健康的 Agent 就是这个状态。
- **已停用**（`inactive`）：令牌已注销，或它的所有者已停用。

界面上跟在状态后面的是「最近活动 …」（心跳与最近一次工具调用里更晚的那个），不写「心跳」。
_Avoid_: 在线、离线、存活、活跃、连接中、心跳（`Heartbeat` 仍是执行记录与 MCP 那一侧的内部词，不进界面）

**最多同时任务数**（code: `MaxConcurrency`）:
一个 Agent 同时可以有几段进行中的执行记录，超过则领取与开始执行都会被拒绝。
_Avoid_: 并发上限、并发数、concurrency

**授权**（code: `Grant`）:
所有者允许 Agent 做的一件事（如领取任务、验收、创建目标）。每项授权可以是"直接生效"或"需要人确认"。
_Avoid_: 权限、scope、permission

**待确认操作**（code: `Proposal`）:
Agent 发起、但要等人点确认才生效的动作。
_Avoid_: 提案、申请、审批单、请求

**能力标签**（code: `Capability`）:
组织负责人维护的一份词表（如编码、写作、数据分析），标注 Agent 会做什么；任务可以用它限定谁能领。
_Avoid_: 技能、skill、tag

**IM 集成**（原名「外部目录」）（code: `Directory`）:
组织在别处维护的组织结构与人员名单，目前支持飞书、企业微信。同步时以IM 集成为组织结构（团队树）与人员名单的来源：新增的人进来是「待激活」成员，离开的人只停用不删除，团队按部门树对齐。
_Avoid_: 通讯录、AD、LDAP、SSO（登录是另一件事）

**提供方**（code: `Provider`）:
IM 集成背后的那个平台（飞书、企业微信…）。一个组织同一时间只用一个提供方；每个提供方自己声明要填哪些凭据、有什么前置条件。换提供方等于换来源：旧来源的团队与成员不停用，改为手工维护。
_Avoid_: 平台、渠道、集成、连接器、source（代码里成员的 source 存的正是提供方代码名）

**企业微信**（code: `wecom`，提供方）:
腾讯的办公套件，用企业 ID 与「通讯录同步」的 Secret 接入。中文写全称「企业微信」；英文用官方名 WeCom。
_Avoid_: 企微、微信、WeChat Work、Wecom

**斜杠命令**（code: `Prompt`）:
系统提供给 Agent 客户端的一组固定操作（领任务、开始执行、提交交付……），在客户端里显示成斜杠命令。人选命令、填参数，Agent 收到的是结构化输入，不靠语义猜测。随 MCP 连接自动出现，不需要额外安装（ADR 0025）。
_Avoid_: 快捷指令、prompt、模板

**只看不做**（code: `DryRun`）:
写操作的一个开关：只回一句"我将要做什么"，不产生任何改动，也不记动态。给 Agent 先念给人听用的，与"需要人确认"是两回事——前者防误解，后者防越权。
_Avoid_: 预演、试运行、dry run

**接入链接**（code: `OnboardLink`）:
一条公开链接（`/connect`），人把它贴给自己的 Agent，Agent 读到的是一份写给它看的操作说明：申请设备码 → 把验证码和网址念给人听 → 等人在网页上确认 → 把令牌写进自己的配置 → 自检并汇报。用浏览器打开则是一张"把这条链接贴给你的 Agent"的页面（ADR 0024）。
_Avoid_: 接入地址、onboarding link、把它叫"注册链接"（注册的是 Agent，不是人）

**接入检查**（code: `Check`，`Diagnose`）:
接IM 集成时系统自己替人查的一组事项（凭据可用、权限范围、部门名称权限、人员姓名权限、邮箱权限、版本已发布…），每项由提供方自己声明，结果是「通过 / 待处理 / 阻塞 / 未检查」。每项带一句"去哪儿点什么"和直达提供方控制台那一页的链接；有阻塞项时不允许预览与同步。界面上的接入向导按它一步步走。
_Avoid_: 诊断、健康检查、自检、探针；权限的英文代号（contact:department.base:readonly 之类）只放进「详情」，不进标题与修法

**同步根部门**（code: `RootDepartmentIDs`）:
从IM 集成同步的范围：空表示整个企业；选了几个部门就只同步它们，各自成为顶层团队（权限范围只含部分部门时从被授权的部门里选）。
_Avoid_: 根节点、scope、范围（"权限范围"是提供方那边的概念，"同步范围"是我们这边选的）

**外部身份**（code: `ExternalIdentity`）:
一个成员或团队在IM 集成里的编号，用来在多次同步之间认出同一个人或同一个部门。
_Avoid_: open_id、union_id、userid（这些是飞书 / 企业微信的字段名，只在代码里出现）

**候选**（code: `MatchCandidate`，`confirm`）:
从 IM 或 CSV 进来的一个人 / 一个团队，系统按认法（邮箱相同、手机号相同、姓名相同且部门与主团队同名、团队同名同层级）觉得可能就是系统里已有的某个对象，但拿不准。候选进预览的「需要你确认」区，由人决定：合并到已有、作为新建、跳过；全部决定完之前不同步。以前绑过外部身份的直接算同一个，不是候选。
_Avoid_: 疑似匹配、匹配项、冲突项、suggestion

**决定**（code: `Decision`，取值 `merge | create | skip`）:
人对一条候选的处理：合并到已有、作为新建、跳过。存成记录，下次预览不再问；跳过的列为「已跳过」，随时可以撤回重新考虑。
_Avoid_: 裁决、审批、resolution

**对应关系**（code: `Mapping`，面板名）:
IM 集成里的对象与系统里对象的绑定表：已绑定（可解绑，解绑后改为手工维护）、可能重复（同步后系统仍用同一套认法在手工对象和同步对象之间找到的疑似重复，可在这里合并）、已跳过（可重新考虑）。
_Avoid_: 映射、绑定表、mapping（界面上）

**合并**（code: `Merge`，`TeamMerged` / `MemberMerged`）:
把两个其实是同一个的对象并成一个。团队合并：旧团队的成员、目标、任务、迭代、共享边界、下级团队整体并入目标团队，旧团队停用不删除。成员合并：目标成员保留自己的角色、Agent 等一切，旧成员的任务、目标、Agent 归到目标身上，旧成员停用；历史不改。合并后若接上了 IM 身份，姓名 / 名称按 IM。
_Avoid_: 去重、归并、合并账号（合并的是成员，不是登录账号）

**可能重复**（code: `Duplicate`，成员页提示「可能与 X 重复」）:
用同一套认法在手工对象与同步对象之间找到的一对疑似同一个的对象。成员页在人名旁给提示，点开就是合并。
_Avoid_: 重名、冲突

**待激活**（code: `PendingActivation`，成员状态）:
从IM 集成同步进来、还没有登录过的成员。有名字有团队，能被指派任务、能收到通知，但要通过邀请链接设密码（或用提供方登录）之后才能进系统。
_Avoid_: 未注册、影子账号

### 目标与任务

**目标**（code: `Goal`）:
组织想达成的一个结果，有负责人，可以逐层拆成子目标；进度由下面的目标和任务汇总，是否达成由负责人确认。不再追求时可以**放弃**（状态 `abandoned`），历史与动态都留着，随时可以重新开始；只有既没有子目标也没有任务的空目标才允许删除。
_Avoid_: Objective、OKR、项目；放弃不要叫归档、关闭、作废

**路线图**（code: `Roadmap`）:
「目标」页下的默认页签，回答"接下来要解决什么、为什么是它、大概什么时候"。形态是一张**时间线**：横轴最细到周（周 / 月 / 季度 / 年四档），一行一个目标，子目标缩进可折叠，里程碑是条上的菱形，条的画法体现日期有多硬。路线图上不画任务，也不出现"某月某日"（里程碑除外）；要看任务级的精确排期去「任务 · 甘特图」（ADR 0021、0022）。目标页只有路线图与树两个页签，原来的「目标 · 甘特图」已并入路线图。
_Avoid_: Roadmap、产品规划、时间表

**时间桶**（code: `Horizon`）:
目标的粗时间表达，只有三档：现在、下一步、以后；可以空着表示还没排期。**日期永远优先**：有计划起止时以日期为准，时间桶只作分组与显示。故意不做成可配置词表，多一档就会有人拿它当季度。
_Avoid_: 阶段、季度、Q1

**时间粒度**（code: `DatePrecision`）:
目标的计划起止填到多细为止：到周、到月、到季度、到半年、到年；空着等于到周，**最细就是到周**。它决定路线图上这根条怎么画（到周吸附到周一与周日、硬边，粗粒度两端吸附到刻度并做雾边），也决定对外分享时显示到什么精度。与信心度正交：可以"季度粒度、高信心"，也可以"到周、低信心"（ADR 0022）。
_Avoid_: 精度、模糊度、日期类型

**排序权重**（code: `Rank`）:
同一条泳道里目标的手动次序，用来表达"这个更要紧"，与日期无关。
_Avoid_: 优先级（优先级是任务上的字段）、权重

**信心度**（code: `Confidence`）:
目标能不能按这个时间桶达成的把握，只有三档：高、中、低；可以空着。只在路线图上呈现，任务与甘特图不继承。
_Avoid_: 把握度、概率、风险等级

**成果指标**（code: `Outcome`）:
一句话说明这个目标达成时什么变了（如「登录转化率从 62% 提到 70%」）。给路线图的读者看，不参与计算。
_Avoid_: KR、指标、度量

**里程碑**（code: `Milestone`）:
目标上的一个有日期的节点，表示"到这一天应该达到什么"，有名称、日期、可选说明；到了日期由目标负责人确认「已达到」，日期过了还没确认就是逾期。里程碑不是任务：没有人去"做"一个里程碑，它是目标在时间轴上的刻度，用来在路线图上看目标是否按节拍推进。
_Avoid_: 节点、检查点、milestone、阶段（阶段专指流程里的状态）

**目标还是任务**（判定规则）:
能指派给某个人或某个 Agent 去做的，是**任务**；没法指派给任何人、只能靠下面的东西汇总出进展的，是**目标**。由此：任务有执行者、有流程状态、有执行记录和成本；目标只有负责人（必须是人），没有流程，进度靠汇总，是否达成由人判断。两者在界面上必须一眼可分（见 `web/DESIGN.md`「目标与任务的区分」）。

**目标类型**（code: `GoalType`）:
目标的分类（产品、项目、经营目标……），由组织自己维护，可改名、可加、可停用。它**只用来分类和显示**：不决定流程（目标本来就没有流程）、不决定权限与成本归口；唯一的附带信息是新建时预填的默认时间粒度。可以空着，显示为「未分类」（ADR 0023）。
_Avoid_: 目标分类、goal type、把它当成流程模板

**目标负责人**（code: `GoalOwner`）:
对一个目标负责的成员。
_Avoid_: owner、目标所有者

**任务**（code: `Task`）:
挂在某个目标下、可以交给人或 Agent 去做的一件事，可以拆出子任务；最底层的任务才是真正被执行的。
_Avoid_: 事项、工单、issue、ticket、story

**任务序号**（code: `TaskNumber`，字段 `number`）:
组织内从 1 递增的整数，界面上写成 `#123`；列表、看板、动态、MCP 返回都带它，输入 `#123` 或 `123` 就能打开任务。`tsk_xxx` 只是代码里的 ID，不给人看。
_Avoid_: 编号、ID、工单号、key

**任务类型**（code: `TaskType`）:
任务的分类（通用、需求、Bug、发布……），决定这个任务走哪套流程、有哪些参与角色、要填哪些字段，以及给 Agent 的执行指令。系统内置四种，组织可改可加。
_Avoid_: 任务模板、issue type

**执行者**（code: `Executor`）:
能被指派任务的对象，人或 Agent 都算。任务不关心执行者是人还是 Agent。
_Avoid_: 处理人、worker

**任务负责人**（code: `Assignee`）:
任务当前指派给的执行者，对结果负责。负责人是人时，他的任何一个 Agent 都可以替他做。
_Avoid_: 受理人、指派人、经办人

**参与角色**（code: `Participant`）:
任务类型规定的、按角色命名的位置（如开发、测试），每个位置填一个执行者。流程走到某一步时，任务负责人会自动换成对应位置上的人；位置空着就进入待领取任务，要求有对应角色的人来领，领取后该位置自动回填（"待回填"）。
_Avoid_: 槽位、slot、干系人、stakeholder

**待领取任务**（code: `Backlog`）:
所有还没有负责人的任务，符合条件的执行者可以自己领。这是一个视图，不是一个容器。
_Avoid_: 任务池、公开池、待办、backlog

**优先级**（code: `Priority`）:
任务的紧急程度，四档：紧急、高、普通、低。影响待领取任务和列表的排序。
_Avoid_: P0/P1、等级、重要性

**逾期**（code: `Overdue`）:
计划结束日期已过、任务还没结束的状态；是派生属性，不是流程状态。
_Avoid_: 超期、延期、delay

**预计工时**（code: `Estimate`）:
任务上可选的工作量估计（小时），用于进度加权；没填按 1 算。
_Avoid_: story point、规模

**关联**（code: `Relation`）:
两个任务之间带类型的关系：「前置」（前面的没完成，后面的不能领；前面的被取消或终止时，前置关系自动解除并留下动态）、「发现于」（Bug 是在哪个任务里发现的）、「相关」。可以跨目标，不允许绕成圈。
_Avoid_: 依赖、dependency、链接、link、blocker

**周期任务**（code: `Schedule`）:
按固定周期自动生成任务的规则（如每周一生成一个"写周报"）。
_Avoid_: 定时任务、cron、循环任务

**任务说明**（code: `TaskBrief`）:
执行者接到任务时看到的完整背景：任务描述、所属目标链、历史评论、前置任务的结果和交付物、任务类型给 Agent 的执行指令。
_Avoid_: 上下文、prompt、简报、任务包

**入口**（导航约定）:
侧栏只有六个入口：我的工作、组织概览、目标、任务、Agent、组织设置。看板、甘特图、迭代、待领取是「任务」下的页签，运营数据是「组织概览」下的页签，流程是「组织设置」下的页签，待确认操作并入「我的工作」的「待我处理」（历史记录保留为子页）。角色不同只改变默认打开的页签与范围，不增减入口（ADR 0015）。

**看板**（code: `Board`）:
按状态分列展示任务的视图：每一列是所选任务类型流程里的一个状态，卡片是任务，把卡片拖到另一列就是执行一次状态变化，不允许的变化会被拒绝并给出理由。不是新的数据，只是任务的另一种看法。
_Avoid_: Kanban、泳道板、运营看板（那是「运营总览」）

**在制品上限**（code: `WIPLimit`）:
流程里某个状态同时容纳的任务数上限，只做提醒不做拦截：看板上超限的列会标红并显示「超出 n」。
_Avoid_: WIP、限流

**迭代**（code: `Sprint`）:
一段固定时间（通常一到两周）内团队承诺完成的一批任务。有名称、迭代目标、起止日期，状态是规划中、进行中、已结束三种，同一时间一个团队只能有一个进行中的迭代。结束时未完成的任务退回待办或转入下一个迭代。
_Avoid_: Sprint、冲刺、周期

**迭代待办**（code: `SprintBacklog`）:
迭代里的任务清单。与「待领取任务」不同：待领取是"谁来做"的问题，迭代待办是"这段时间做什么"的问题。
_Avoid_: 需求池、product backlog

**工作量**（code: `Points`）:
任务的相对大小估计，整数点数（1、2、3、5、8、13），由团队自己校准；燃尽图和迭代速度按它计算。与「预计工时」并存：工时是时间，工作量是相对大小。
_Avoid_: 故事点、story point、估点

**燃尽图**（code: `Burndown`）:
迭代内每天剩余工作量（或任务数）的折线，配一条从起点到终点的理想线。
_Avoid_: burn-down chart

**迭代速度**（code: `Velocity`）:
最近几个已结束迭代平均完成的工作量，用来估计下一个迭代能装多少。
_Avoid_: velocity、产能

**运营总览**（code: `Dashboard`）:
成本、周期、吞吐与 Agent 表现的汇总页。原名「运营看板」，为避免与「看板」混淆而改名。
_Avoid_: 看板、仪表盘、dashboard

**我的工作**（code: `MyWork`，导航入口）:
每个人登录后的第一屏：整页是按角色配置、可自己拖动调整的工作台区块，「待我处理」与「组织概况」默认排在最上面。原名「首页」。
_Avoid_: 首页、仪表盘、dashboard

**待我处理**（code: `Inbox`）:
「我的工作」顶部的固定区，汇总所有等我出手的事：等我确认的待确认操作、等我验收的任务、我负责但逾期的任务、指派给我但没开始的任务、等我答复的提问、未读通知。每条都能就地处理；只看本人，不受范围影响。不能被隐藏或挪走。
_Avoid_: 收件箱、inbox、消息中心、通知中心

**工作台**（code: `Workspace`）:
「我的工作」里「待我处理」下面的可配置部分，由若干区块按顺序组成。布局按角色配置，成员可以微调自己的。
_Avoid_: 仪表盘、dashboard、首页配置

**区块**（code: `Block`）:
工作台上的一块，系统定义的有限集合，例如「我的任务」「要我关注的异常」「成本与预算」。每个区块服从当前范围。
_Avoid_: 组件、widget、卡片

**预设**（code: `Preset`）:
系统发的一套区块组合，按这类人关心什么命名：全局视角、部门视角、小组视角、执行视角、运营视角。组织把预设套到自己的角色上再改。
_Avoid_: 模板、角色模板、persona

**显示偏好**（code: `Preferences`）:
一个人怎么看界面的选择：任务列表显示哪些列、看板卡片显示哪些字段、默认打开哪个任务视图、是否紧凑、侧栏是否默认收起。解析顺序与工作台一样：个人 → 角色（组织给这类人配的默认）→ 系统默认；可一键重置。列与字段的集合是系统定义的，不改任何数据，也不是权限。
_Avoid_: 设置（那是组织设置）、个性化、配置、视图配置

**通知通道**（code: `NotifyChannel`，取值 `feishu | wecom | email | webhook`）:
把站内通知送到系统外面的一条路：飞书私聊、企业微信应用消息、邮件、webhook。组织在「组织设置 → 通知」里决定开哪些通道、哪些事件类型可以外发（ADR 0019）；飞书、企业微信的通道复用「IM 集成」的凭据与接入检查（多一项「能以应用身份发消息」），邮件与 webhook 单独配。站内「待我处理」永远是事实源，通道只是复印件。
_Avoid_: 渠道、推送、消息通道、channel（界面上）、集成（那是 IM 集成）

**投递**（code: `Delivery`）:
一条通知经一个通道发给一个人的一次记录：事件类型、通道、状态（排队中 / 已发出 / 失败 / 未发）、错误原话、发了几次、什么时候发出。同一事项、同一收件人、同一通道只有一条。个人和组织都能在通知设置里看最近的投递；连续失败的通道在检查清单里亮黄。
_Avoid_: 推送记录、发送日志、日志、log

**安静时段**（code: `QuietHours`）:
一个人在「我的偏好 → 通知」里设的一段时间（如 22:00 到 08:00），这段时间里不外发，到点再发；站内不受影响。
_Avoid_: 免打扰、勿扰模式、静音、DND

### 执行与流程

**流程**（code: `Workflow`）:
绑定在任务类型上的一套状态和步骤：有哪些状态、每一步谁能走、走之前要满足什么、走完后负责人换成谁。流程有版本，进行中的任务沿用创建时的版本。
_Avoid_: 审批流、流程引擎、pipeline、BPMN、状态机

**状态类型**（code: `StateLabel`）:
流程里每个状态必须归入的五类之一：未开始、进行中、等待中、已完成、已终止。未开始的状态可以标记为"可领取"。系统只认状态类型，不认状态的名字。
_Avoid_: 语义标签、label、category

**阶段**（code: `Stage`）:
流程里一个"进行中"状态在界面上的叫法，用于说明一段执行记录属于哪个阶段（设计中、开发中……）。
_Avoid_: 环节、步骤（步骤专指流程里的迁移）

**执行记录**（code: `Run`）:
某个执行者在任务的某个"进行中"阶段实际干活的一段记录，从他自己开始（进入阶段、领取或点"开始执行"）到离开这个阶段为止。一个任务可以有多段执行记录，同一时刻最多一段在进行。任务处于"进行中"但没有执行记录是正常的（已交接、还没开始）。
_Avoid_: Run、job、session、attempt、执行

**用量**（code: `Usage`）:
一段执行记录上报的原始消耗：按模型分别记录 token 数、耗时、工具调用次数，另有 token 总和。用量不含金额。
_Avoid_: 消耗、开销、billing

**成本**（code: `Cost`）:
系统按版本化的价格表把用量折算成的钱。产生了就算，不等验收。
_Avoid_: 费用、花费

**结果**（code: `Result`）:
一段执行记录提交的成果：文字摘要、交付物列表，以及任务类型规定的字段。人和 Agent 提交的结果结构一样。
_Avoid_: 输出、deliverable

**交付物**（code: `Artifact`）:
结果里带类型的具体产出，如 PR、文档、报表、文件。流程可以规定某一步必须带某类交付物。
_Avoid_: 产出物、附件、attachment

**验收**（code: `Acceptance`）:
验收人确认提交的结果可以接受，任务只有验收后才算完成。
_Avoid_: 审核、审批、approve、review

**验收人**（code: `Reviewer`）:
对某个任务有验收权的执行者（人或 Agent）。默认是任务创建者，任务类型可以改。创建者自己做的任务可以自己验收。
_Avoid_: 审核人、审批人、approver

**评论**（code: `Comment`）:
任务上需要人看的一条对话，人和 Agent 都能发，会触发通知。Agent 用它提问并让任务进入等待。
_Avoid_: 留言、消息、备注

**工作日志**（code: `Note`）:
Agent 在任务上留下的过程记录，默认折叠，不打扰人。
_Avoid_: 日志、log、note

**预算**（code: `Budget`）:
目标上可选的成本上限，超了只提醒，不拦任何操作。
_Avoid_: 限额、配额、quota、硬上限

### 代码平台

**代码平台**（code: `CodePlatform`）:
组织接进来的代码托管平台（GitHub、GitLab、Gitee），系统从它收 PR 与检查结果，用来自动推进任务、给任务挂链接。它和「IM 集成」一样走同一套接入向导：选平台 → 填令牌 → 检查 → 选仓库。系统不托管代码、不配置 CI、不在里面评审代码。
_Avoid_: Git 平台、代码仓库、代码托管、SCM、VCS

**外部链接**（code: `ExternalLink`）:
任务上挂的一条站外地址：PR、Issue、文档、设计稿或其他。PR 类的由代码平台自动挂上并跟着更新状态（打开 / 草稿 / 已合并 / 已关闭），其余手工添加。
_Avoid_: 关联链接、附件链接、引用（「关联」专指任务之间的关系）

**外部事件**（code: `ExternalEvent`）:
代码平台上发生的一件事：PR 打开、PR 可评审、PR 合并、PR 关闭、检查通过、检查失败。流程里的一步可以声明由某件外部事件触发，条件满足时系统自己走这一步并写清"谁在哪儿做了什么"。
_Avoid_: webhook、钩子、回调（「回调地址」只在接入设置里出现）

### 记录

**动态**（code: `DomainEvent`）:
系统里每一次变化的不可更改的记录，只增不改。通知、Webhook、统计和所有视图都从它派生。
_Avoid_: 事件、event、日志、操作记录、audit log
